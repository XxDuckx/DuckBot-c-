<UserControl x:Class="DuckBot.GUI.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#1e1e1e" Foreground="White">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <StackPanel Background="#252525" Grid.Column="0">
            <Button Content="General" Click="General_Click" Style="{StaticResource SidebarButtonStyle}"/>
            <Button Content="Advanced" Click="Advanced_Click" Style="{StaticResource SidebarButtonStyle}"/>
            <Button Content="Solvers" Click="Solvers_Click" Style="{StaticResource SidebarButtonStyle}"/>
            <Button Content="Repository" Click="Repository_Click" Style="{StaticResource SidebarButtonStyle}"/>
            <Button Content="Backups" Click="Backups_Click" Style="{StaticResource SidebarButtonStyle}"/>
        </StackPanel>

        <!-- Content -->
        <ContentControl x:Name="ContentArea" Grid.Column="1" Margin="10"/>
    </Grid>
</UserControl>
