<UserControl x:Class="DuckBot.GUI.Views.ScriptBuilderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#1e1e1e" Foreground="White">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <ComboBox x:Name="GameSelect" Width="150" Margin="0,0,8,0"/>
            <Button Content="New" Width="70" Margin="0,0,8,0"/>
            <Button Content="Load" Width="70" Margin="0,0,8,0"/>
            <Button Content="Save" Width="70" Margin="0,0,8,0"/>
            <Button Content="Export JS" Width="100" Margin="0,0,8,0"/>
            <Button Content="Test Run" Width="100" Margin="0,0,8,0"/>
            <Button Content="Capture Screenshot" Width="150" Margin="0,0,8,0"/>
            <Button Content="Crop from Screenshot" Width="170"/>
        </StackPanel>

        <!-- Main Panels -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <!-- Actions Library -->
            <ListBox x:Name="ActionsLibrary" Grid.Column="0" Background="#252525" BorderBrush="#333"
                     Margin="0,0,8,0"/>

            <!-- Step Sequence -->
            <ListBox x:Name="StepSequence" Grid.Column="1" Background="#1e1e1e" BorderBrush="#333"
                     Margin="0,0,8,0" />

            <!-- Step Inspector -->
            <StackPanel x:Name="Inspector" Grid.Column="2" Background="#252525" BorderBrush="#333">
                <TextBlock Text="Step Inspector" FontWeight="Bold" Margin="8"/>
                <!-- Dynamic controls added at runtime -->
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
