<UserControl x:Class="DuckBot.GUI.Views.Settings.AdvancedSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#1e1e1e" Foreground="White">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="10">
            <TextBlock Text="Advanced Settings" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>

            <CheckBox x:Name="RestartReconnected" Content="Restart running instances that reconnected" Margin="0,4"/>
            <CheckBox x:Name="KillBridge" Content="Kill bridge if emulator stays offline" Margin="0,4"/>
            <CheckBox x:Name="RestartGame" Content="Restart game if home screen is visible" Margin="0,4"/>
            <CheckBox x:Name="SkipResCheck" Content="Skip emulator display resolution check" Margin="0,4"/>
            <CheckBox x:Name="SkipUpdates" Content="Skip bot updates and changelog parsing" Margin="0,4"/>
            <CheckBox x:Name="RestartAfterTimeout" Content="Restart emulator after script timeout" Margin="0,4"/>
            <CheckBox x:Name="RemoveCooldown" Content="Remove scripts currently on cooldown from queue" Margin="0,4"/>
            <CheckBox x:Name="CloseUnused" Content="Close emulators not being botted on" Margin="0,4"/>

            <StackPanel Orientation="Horizontal" Margin="0,8">
                <TextBlock Text="Condition check delay (ms):" Width="250"/>
                <TextBox x:Name="DelayBox" Width="100" Text="200"/>
            </StackPanel>

            <Button x:Name="SaveButton" Content="Save" Width="100" Margin="0,10,0,0" HorizontalAlignment="Right"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
