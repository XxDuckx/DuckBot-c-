<UserControl x:Class="DuckBot.GUI.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="400" d:DesignWidth="600">
    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="1" Text="Username" FontSize="16" Margin="0,0,0,5"/>
        <TextBox x:Name="UsernameBox" Grid.Row="2" Height="35" FontSize="16" Padding="6"/>

        <TextBlock Grid.Row="3" Text="Password" FontSize="16" Margin="0,12,0,5"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="4" Height="35" FontSize="16" Padding="6"/>

        <Button Grid.Row="4" VerticalAlignment="Bottom" Margin="0,70,0,0"
                Height="42" Width="160" Content="Login" Click="Login_Click"
                Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Left"/>
    </Grid>
</UserControl>
